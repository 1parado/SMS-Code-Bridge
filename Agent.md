# Agent.md — SMS Code Bridge

## 项目目标
构建一个极简、轻量、本地优先的验证码转发工具：
- Android 端监听短信验证码
- 通过局域网安全推送到 Windows 端
- Windows 端自动复制到剪贴板并轻量提示
- 仅服务「投简历 / 注册 / 登录」等高频验证码场景

## 技术栈强制要求
- **Windows 端**：Rust（必须）
- **Android 端**：Java / Kotlin（必须）
- 禁止引入大型框架或不必要依赖
- 最终产物体积各自严格控制在 **20MB 以内**

## 目录结构（强制）
仓库根目录只放文档（Agent.md / PRD.md / README.md）与 CI 配置，代码一律按端分目录：
- `computer/` —— **电脑端（Windows）代码**，Rust 工程根目录放在这里
- `phone/` —— **手机端（Android）代码**，Java / Kotlin 工程根目录放在这里

规则：
- 严禁在仓库根目录新建任何源码文件、Cargo 工程或 Gradle 工程
- 两端各自独立构建，互不依赖对方目录
- 两端共用的产物（协议定义、示例配置）如需共享，放在根目录 `shared/`，且只放纯文本定义，不放实现代码

## 核心原则
1. **极简**：两端 UI 和功能都做到最少必要
2. **轻量**：内存占用低、启动快、后台几乎无感
3. **本地优先**：所有数据只存在本机，不经过任何公网服务器
4. **最小权限**：只申请真正需要的权限，绝不越权
5. **不影响其他应用**：Android 端不得干扰正常短信接收、通知或其他 App
6. **安全配对**：仅通过局域网配对，使用临时密钥 / 配对码，防止未授权连接
7. **传输尽可能快**：手机端收到验证码后到电脑端完成复制的全链路延迟必须尽可能低（目标控制在毫秒到极低百毫秒级），优先使用高效局域网协议与最小数据包

## 权限规范（强制）

### Android 端（只允许以下权限）
- `RECEIVE_SMS`（或等效短信接收权限）
- `READ_SMS`（仅用于读取验证码短信，处理完立即释放）
- `INTERNET`（仅用于局域网通信）
- `ACCESS_WIFI_STATE` / `ACCESS_NETWORK_STATE`（检测局域网状态）
- 禁止申请：存储、通讯录、位置、相机、麦克风、通知监听（除非绝对必要且可关闭）、后台无限制运行等高危权限

### Windows 端
- 仅需要网络权限（局域网）
- 剪贴板读写
- 系统托盘
- 禁止请求管理员权限（除非用户主动选择）

## 安全要求
- 配对必须通过局域网完成（二维码 / 短配对码）
- 使用一次性或短时有效的配对密钥
- 通信内容仅包含验证码数字（脱敏），不传输完整短信原文
- 支持一键断开 / 清除配对
- 默认只处理包含「验证码」「校验码」「code」等关键词的短信
- 可配置白名单关键词或发件人（可选，默认关闭）

## 功能边界（严格遵守）
### 必须实现
- Android 监听并提取验证码
- 局域网实时、尽可能快地推送到已配对的 Windows 端（优先低延迟协议与最小数据包）
- Windows 端自动复制验证码到剪贴板
- 轻量托盘提示（可关闭）
- 配对与解绑
- 本地历史记录（仅验证码 + 时间，可清空）

### 明确禁止
- 云同步、账号系统、远程服务器
- 完整短信转发
- 任何形式的数据上传
- 复杂 UI、多页面、设置页堆砌
- 影响其他应用正常使用短信/通知

## 开发节奏与 PR 规范（强制）
- 需求按 `docs/PR-PLAN.md` 拆成小 PR，**一个 PR 只做一件事**，可独立合并、独立测试
- **每个 PR 必须带测试用例**：业务逻辑写成纯函数（便于 Rust / JVM 单测），平台相关能力（剪贴板、短信广播、托盘）用接口抽象 + 假实现注入
- 测试夹具放在 `shared/testdata/`，两端共用，保证协议与解析行为一致；夹具内容必须脱敏

## 构建与发布规范（强制）
- **本地禁止编译、打包**：不得在本地执行 `cargo build`、`gradlew assemble*` 等构建命令，本地只写代码与测试
- 构建、测试、打包**全部由 GitHub Actions 完成**
- 日常 PR 只跑检查与测试（Rust：`fmt --check` / `clippy -D warnings` / `cargo test`；Android：`testDebugUnitTest` / `lintDebug`），**不出任何包**
- **只有里程碑大版本完成**并打 `vX.Y.Z` tag 时才触发编译 + 体积门禁 + 发布 Release
- 仅支持 Android + Windows
- Release 只发布两个文件：
  1. Windows 端可执行文件（.exe 或便携包）
  2. Android 端 APK
- 两个产物体积均 ≤ 20MB（CI 硬性门禁，超限直接失败）

## UI / UX 强制风格
- 参考极简白色界面（大量留白、圆角、轻阴影）
- 默认几乎无界面存在感（托盘为主）
- 任何弹窗 / 提示必须极简、短暂、可关闭
- 禁止花哨动画、重色、复杂布局

## 数据存储
- 全部本地
- Windows：本地配置文件 + 简单历史（可清空）
- Android：SharedPreferences 或极简本地文件
- 不使用数据库（除非必要且极轻量）

## 开发约束
- 代码必须可读、模块清晰
- 优先使用标准库和成熟极轻量 crate / 库
- 任何新增依赖必须评估体积影响
- 保持「能不做就不做」的克制原则

## 代码变更后的敏感信息检查（强制流程）
**每次新增 / 修改代码后、提交（commit）之前，必须执行以下流程：**

1. **扫描敏感信息**：检查本次变更涉及的所有文件（含新建文件），逐项排查是否包含：
   - 密钥、Token、API Key、配对密钥、签名密钥、加密盐值
   - 密码、数据库连接串、含凭据的 URL
   - 局域网 IP / MAC / 设备唯一标识、主机名
   - 真实手机号、邮箱、短信原文样例、真实验证码
   - 签名文件：`.jks` / `.keystore` / `.p12` / `.pfx` 及其口令
   - 本地环境变量文件（`.env`）与本地配置文件
2. **优先改写而不是忽略**：能改成占位符、示例值或运行时注入的，先改代码本身（如 `192.168.1.100` → `192.168.x.x`，`+8613800138000` → `+8613800000000`），敏感值一律走环境变量或本地未提交的配置模板。
3. **确认必须留在工作区但绝不能入库的文件**（如签名 keystore、本地配置、测试用真实短信样本），立刻把**具体文件名或路径**追加到仓库根目录 `.gitignore`，而不是用宽泛通配符，避免误伤源码。
4. **校验生效**：追加后执行 `git check-ignore -v <文件>` 确认该文件确实被忽略；若文件已被 Git 跟踪，需先 `git rm --cached <文件>` 再提交，并向用户说明。
5. **禁止提交**：绝不允许把上述任何内容写进代码、注释、测试用例或提交信息。发现已提交的敏感信息，立即从源码中移除并按泄露处理，不保留历史明文。

### .gitignore 维护要求
- `.gitignore` 位于仓库根目录，由本项目唯一维护，不新增子目录里的 `.gitignore`（除非工程本身强制需要）
- 追加格式：按 `computer/`（Rust）与 `phone/`（Android）分组填写，并在条目后加简短中文注释说明原因
- 已忽略的文件若被误加入，按第 4 步纠正，不用删除 `.gitignore` 条目的方式绕过
