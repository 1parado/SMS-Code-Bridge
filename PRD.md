# 产品需求文档（PRD）
## 产品名称（暂定）
**CodeBridge** / 验证码桥（最终命名可再定）

## 一句话定位
一个极简、本地、安全的验证码自动转发工具：手机收到验证码后，电脑自动复制，投简历再也不用反复看手机。

## 目标用户
- 正在投递简历、频繁注册/登录网站的职场人与学生
- 希望减少「看手机 → 记验证码 → 切回电脑输入」这个重复动作的人
- 对隐私敏感，不希望验证码经过第三方服务器的人

## 核心价值
- **高频场景提效**：投简历、注册、登录时节省大量重复操作
- **极低存在感**：平时几乎感觉不到它的存在
- **隐私安全**：数据只在本地局域网流转，不经过公网
- **真正轻量**：两端都极简，体积小、资源占用低

## 产品形态

### Windows 端
- 单文件 / 便携可执行程序（.exe）
- 默认以**系统托盘**形式常驻
- 主界面极简（参考设计图风格）：大面积留白、圆角卡片、底部简洁输入/状态区域
- 验证码到达时：
  1. 自动复制到剪贴板
  2. 可选右下角极简 Toast 提示（可关闭）
  3. 托盘图标短暂变化提示

### Android 端
- 极简 APK
- 安装后引导完成局域网配对
- 之后主要在后台运行
- 界面只有：配对状态、简单开关、历史验证码列表（可选）、清空数据
- 不影响系统短信正常接收与其他 App 使用

## 功能列表

### 1. 配对与连接（必须）
- 两端通过局域网发现与配对
- 使用短配对码或二维码完成首次连接
- 支持一键断开 / 重新配对
- 显示当前连接状态（已连接 / 未连接）

### 2. 验证码接收与转发（核心）
- Android 端监听短信
- 自动识别包含验证码关键词的短信
- 提取纯数字验证码
- 通过已配对的局域网通道**尽可能快**地实时推送到 Windows（目标全链路低延迟，优先高效协议与最小数据包）
- 只传输验证码本身，不传输完整短信内容

### 3. Windows 端处理
- 收到验证码后自动写入系统剪贴板
- 可选轻量提示（Toast / 托盘气泡）
- 本地保存最近验证码历史（时间 + 验证码），支持一键清空
- 支持快捷键查看最近一条验证码（可选）

### 4. 安全与隐私
- 所有通信仅限局域网
- 配对使用临时密钥
- 支持手动清除所有本地数据
- 默认只处理验证码类短信

### 5. 设置（极简）
- 开关：自动复制
- 开关：提示音 / Toast
- 清空历史
- 断开配对
- 关于 / 版本信息

## 明确不做的功能（边界）
- 云同步、账号登录、远程推送
- 完整短信转发或备份
- 多设备同时在线复杂管理
- 复杂过滤规则编辑器
- 主题商店、皮肤、动画特效
- 任何需要额外高权限的功能

## UI / 设计规范（参考提供图片）
- **整体风格**：极简、大量留白、浅色、圆角、轻阴影
- **主色**：白 / 极浅灰背景 + 少量柔和强调色（如淡蓝按钮）
- **布局**：
  - 左侧可有极简图标栏（可选）
  - 中间大面积留白或状态展示
  - 底部简洁输入/状态条风格区域
- **交互**：
  - 默认几乎无界面，以托盘为主
  - 任何弹出提示必须短暂、可关闭、不打断
  - 按钮和卡片使用大圆角、轻量阴影
- **禁止**：深色模式优先、复杂导航、多层级页面、重视觉元素

## 技术实现要点（与 Agent.md 对齐）
- Windows：Rust
- Android：Java / Kotlin
- 体积各自 ≤ 20MB
- GitHub Actions 自动构建
- Release 只发布 Windows 安装包/exe + Android APK
- 最小权限原则
- 数据全部本地存储
- 不影响手机其他应用正常使用短信

## 成功标准
1. 用户完成配对后，手机收到验证码 → 电脑剪贴板自动出现，全程无需手动操作
2. 两端体积均控制在 20MB 以内
3. 日常后台运行几乎无感知
4. 验证码不离开本地设备与局域网
5. 界面符合极简白色设计风格

## 版本规划（初期）
- **v0.1**：完成配对 + 验证码转发 + 自动复制（核心闭环）
- **v0.2**：历史记录、提示开关、清空数据
- **v0.3**：体验打磨与体积优化

---

**文档状态**：初稿  
**对应规范**：详见 `Agent.md`
